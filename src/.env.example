# Gmail OAuth credentials
# Option A: set these env vars directly
# Option B: place the downloaded JSON from Google Cloud Console at
#            .credentials/gmail-oauth-client.json — no env vars needed
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
